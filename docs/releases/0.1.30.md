# 0.1.30 — Personal BYOK

See ../BYOK.md for the direct-device architecture, commands and boundaries.
The user explicitly authorized reusing the existing WSL key. It was not copied
or displayed. Metadata-only processing is unchanged; Windows Full now passes
--byok explicitly. Managed/company gateway implementation remains future work.

## Verification

- Full suite: 512 tests passed before additional focused edge-case tests.
- Windows policy suite: 959 assertions passed, including BYOK progress counts.
- Direct-image, bounded concurrency, local quota, interrupted-call handling,
  claim ownership, duplicate completion, offline result display, Remote-source
  eligibility and legacy-cloud coexistence are covered with mock providers.
- Production BYOK endpoint deployed; authenticated health and invalid-payload
  schema checks passed without creating jobs.
- Real canary: the same one-photo job was attempted twice, one worker, no backlog.
  Both reached OpenAI and were rejected. The precise provider code on retry was
  credit_balance_exhausted, previously mislabeled OpenAIRateLimited. No successful
  live description/result synchronization is claimed. The photo remains resumable.
- Credit exhaustion and organization/project spend limits now pause as billing
  conditions rather than entering ordinary rate-limit retry loops.
- Initial installer candidates were held by a baseline drag-animation probe
  failure. This gate is now resolved by continuous observation and bounded
  completion waiting; no motion assertion was weakened or bypassed.

## Initial external blocker (resolved)

Add prepaid credits for the organization owning this API key in OpenAI billing.
Then rerun the one-photo canary before a large catch-up. The app's US$230 guard
does not fund the OpenAI account. No further live attempts should run until the
balance is restored. Existing managed-cloud work remains separate from BYOK.

## Funded live verification

After the user added credits, the same one-photo canary completed successfully
with one worker using gpt-5.6-terra. Local journal state is Synced and the backend
job is Succeeded. Backend media-detail text and the Windows desktop bridge both
match the locally saved result. Usage: 924 input tokens, 22 output tokens, zero
cached/reasoning tokens; accounted Standard-rate cost US$0.002112. No backlog
was started. This resolves the live BYOK verification blocker, but does not
resolve the independent installer animation-test gate by itself; that gate was
subsequently resolved and verified below.

## Final installer release

- Signed installer: `installer/NektronMoments.Setup.0.1.30.exe` (73,667,496 bytes).
  Valid timestamped Nektron, Inc. signature.
- SHA256: `12881713D012789465AF85A94EF26EC3C038EDC348220A058C3C0D3918EB5495`.
- 516 repository tests and 959 Windows policy assertions passed.
- Published and installed copies passed 100,000-item scrolling, 32 photo-opening
  checks, 132 browsing/workflow checks, artwork validation, native launch,
  workspace reconnection and the isolated install/uninstall canary.
- Animation diagnostics now sample continuously while recording genuine XAML
  transforms. Two diagnostic repeats captured 4 and 11 intermediate positions,
  respectively, with all eight animations completed and retired in each run.
- A separate installed-copy wheel failure was traced to an actual deactivation
  Stop during the synthetic wheel sequence. Interrupted trials are recorded and
  retried at most three times; every frequency must complete an uninterrupted
  trial and pass the unchanged motion/endpoint assertions. Final installed-copy
  verification passed all 16 wheel trials with zero interrupted attempts.
- Real-photo verification covered 144,881 catalog items and 12 motion checks at
  three sizes. Saved order unchanged; no AI jobs started. UI dispatcher wait p95
  was 22.3ms in that run; this is not GPU-present FPS or physical mouse replay.
- Production animation duration and scroll behavior are unchanged. The fixes
  concern measurement and isolation of the native release checks.
